export enum TIME_FORMAT {
	TWELVE_HOUR = "12-hour",
	TWENTY_FOUR_HOUR = "24-hour",
}

export enum BOOKMARK_SOURCE {
	ALL = "all",
	GROUP = "group",
}

export enum QUOTE_SOURCE {
	QUOTEABLE = "Quoteable",
	MY_QUOTES = "My quotes",
	BOTH = "Both",
}
